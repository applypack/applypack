# §8 — Simplify "Bring your own AI"

**Verdict: done.**

## What the plan proposes

Lead with "Use the AI you already have", list the six options, say "ApplyPack
does not sell AI tokens", keep failover detail in docs.

## What the repo has today

- Landing `#open`: *"You bring the AI: a subscription you already pay for, an
  API key, or a local model that costs nothing."* Engine names appear twice
  on the page (Claude Code, Codex, Gemini, local model ×4).
- README "Bring your own AI" section lists the five backends and the local
  route; "What it costs" is its own section.
- Failover detail lives in `docs/ai-engines.md` and on `/settings` → AI
  engine, as the plan wants.

## Steps

None.
