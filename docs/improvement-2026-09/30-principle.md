# §30 — Product principle going forward

**Verdict: adopt the sentence into PRODUCT.md (one line, P3); it is the rule the project already applies.**

*"Does this reduce the amount of manual guessing in a job search without
taking ownership of the decision away from the user?"*

The repo has applied it without naming it: ADR 0012 (the model marks facts,
code scores), ADR 0045 (the text decides presence), ADR 0047/0052 (a person
decides; calibration never re-weights), the F19 closure (no analytics over
noise), gotcha 11 (a rule the user can lose data to is code). PRODUCT.md's
"The task (Operate mode)" section is where it belongs, as the test for a new
feature request — next to the existing *"Nothing on the surface persuades or
markets"*.

## Steps → TASKS §20, block `docs-drift`

- [ ] One sentence in PRODUCT.md under "The task", worded as above, with the
      ADRs that embody it.
